{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks"
  },
  "deploy": {
    "startCommand": "medusa db:migrate && medusa start",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300
  }
}