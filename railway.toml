[build]
builder = "nixpacks"
buildCommand = "echo 'No build needed for test server'"

[deploy]
rootDirectory = "backend"
startCommand = "node test-server.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
healthcheckPath = "/health"
healthcheckTimeout = 30
numReplicas = 1